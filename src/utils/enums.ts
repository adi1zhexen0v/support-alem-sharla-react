export enum MessageTypes {
  TEXT = 'text',
  IMAGE = 'image',
  FILE = 'file',
}

export enum StatusTypes {
  NEW = 'new',
  PROCESS = 'inProcess',
  COMPLETED = 'completed'
}

export enum StatusRusTypes {
  NEW = 'Новые',
  PROCESS  = 'В процессе',
  COMPLETED = 'Завершенные'
}